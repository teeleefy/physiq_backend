{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "http://example.com/example.json",
    "type": "object",
    "default": {},
    "title": "Goal Schema",
    "required": [],
    "properties": {
        "goal_name": {
            "type": "string",
            "minLength": 2,
            "maxLength": 100,
            "title": "The goal_name Schema",
            "examples": [
                "Make dentist appointment"
            ]
        },
        "goal_details": {
            "type": "string",
            "maxLength": 150,
            "title": "The goal_details Schema",
            "examples": [
                "Need to make appointment by end of July. Would like to get a cleaning before end of September."
            ]
        }
    },
    "examples": [{
        "goal_name": "Make dentist appointment",
        "goal_details": "Need to make appointment by end of July. Would like to get a cleaning before end of September."
    }]
}